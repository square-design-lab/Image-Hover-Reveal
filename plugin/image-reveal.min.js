(function (window, document) {  'use strict';  var ID_PATTERN = /^image-reveal(?:-\d+)?$/;  var MOBILE_QUERY = '(max-width: 900px)';  var REDUCED_MOTION_QUERY = '(prefers-reduced-motion: reduce)';  var SPEED_MAP = {    slow: 850,    medium: 650,    fast: 420  };  var defaults = {    target: null,    instanceId: null,    layout: 'left-content-right-image',    animation: {      preset: 'fade-slide-scale',      speed: 'medium',      durationMs: 650,      easing: 'cubic-bezier(0.22, 1, 0.36, 1)'    },    reveal: {      mode: 'fixed',      followCursor: false,      drift: false,      rotationDeg: 4,      hoverLiftY: 24,      offsetRightPx: 80,      offsetX: 0,      offsetY: 0    },    textEffects: {      rollover: false,      inwardSlidePx: 10    },    rowHover: {      bgEnabled: false,      bgColor: 'rgba(255,255,255,0.06)'    },    mobile: {      showEffectOnMobile: false,      accordionMode: false,      accordionSingleOpen: true,      forceMode: 'auto'    }  };  var instances = new Map();  var uid = 0;  function isObject(value) {    return value && typeof value === 'object' && !Array.isArray(value);  }  function deepMerge(target, source) {    var output = Object.assign({}, target);    if (!isObject(source)) {      return output;    }    Object.keys(source).forEach(function (key) {      var sourceValue = source[key];      var targetValue = output[key];      if (isObject(sourceValue) && isObject(targetValue)) {        output[key] = deepMerge(targetValue, sourceValue);      } else {        output[key] = sourceValue;      }    });    return output;  }  function clamp(value, min, max) {    return Math.min(max, Math.max(min, value));  }  function normalizeConfig(config) {    var merged = deepMerge(defaults, config || {});    if (merged.reveal.followCursor) {      merged.reveal.mode = 'followCursor';    } else if (merged.reveal.drift) {      merged.reveal.mode = 'drift';    }    if (!SPEED_MAP[merged.animation.speed]) {      merged.animation.speed = merged.animation.speed === 'custom' ? 'custom' : 'medium';    }    if (merged.animation.speed !== 'custom') {      merged.animation.durationMs = SPEED_MAP[merged.animation.speed] || SPEED_MAP.medium;    } else {      var parsedDuration = Number(merged.animation.durationMs);      merged.animation.durationMs = Number.isFinite(parsedDuration) ? clamp(parsedDuration, 120, 4000) : 650;    }    merged.reveal.rotationDeg = Number(merged.reveal.rotationDeg) || 0;    merged.reveal.hoverLiftY = Number(merged.reveal.hoverLiftY) || 0;    merged.reveal.offsetRightPx = Number(merged.reveal.offsetRightPx) || 0;    merged.reveal.offsetX = Number(merged.reveal.offsetX) || 0;    merged.reveal.offsetY = Number(merged.reveal.offsetY) || 0;    merged.textEffects.inwardSlidePx = Number(merged.textEffects.inwardSlidePx) || 0;    return merged;  }  function nextInstanceId(base) {    uid += 1;    return (base || 'irh') + '-' + uid;  }  function asArray(value) {    if (!value) {      return [];    }    if (Array.isArray(value)) {      return value;    }    if (value instanceof NodeList || value instanceof HTMLCollection) {      return Array.prototype.slice.call(value);    }    return [value];  }  function resolveContainers(target) {    if (!target) {      return Array.from(document.querySelectorAll('[id]')).filter(function (el) {        return ID_PATTERN.test(el.id);      });    }    if (typeof target === 'string') {      return Array.from(document.querySelectorAll(target));    }    return asArray(target).filter(function (node) {      return node && node.nodeType === 1;    });  }  function uniqueElements(elements) {    var seen = new Set();    var out = [];    elements.forEach(function (el) {      if (!el || seen.has(el)) {        return;      }      seen.add(el);      out.push(el);    });    return out;  }  function findSummaryBlocks(container) {    var selectors = '.sqs-block.summary-v2-block, .sqs-block-summary-v2';    var blocks = [];    if (container.matches && container.matches(selectors)) {      blocks.push(container);    }    blocks = blocks.concat(Array.from(container.querySelectorAll(selectors)));    if (!blocks.length) {      var fallback = container.querySelector('.summary-block-wrapper') || container.querySelector('.summary-item-list');      if (fallback) {        blocks.push(fallback.closest('.sqs-block') || fallback);      }    }    return uniqueElements(blocks).filter(function (block) {      return !!block.querySelector('.summary-item');    });  }  function pickFromSrcset(srcset) {    if (!srcset) {      return '';    }    var entries = srcset      .split(',')      .map(function (part) {        return part.trim().split(/\s+/)[0];      })      .filter(Boolean);    return entries.length ? entries[entries.length - 1] : '';  }  function extractBackgroundImage(node) {    if (!node) {      return '';    }    var style = node.getAttribute('style') || '';    var backgroundImage = '';    if (!style && window.getComputedStyle) {      backgroundImage = window.getComputedStyle(node).backgroundImage || '';      style = 'background-image:' + backgroundImage;    }    var match = style.match(/background-image\s*:\s*url\((['"]?)(.*?)\1\)/i);    if (!match && backgroundImage) {      match = backgroundImage.match(/url\((['"]?)(.*?)\1\)/i);    }    return match ? match[2] : '';  }  function dedupeText(values) {    var seen = new Set();    return values.filter(function (value) {      var normalized = value.trim();      if (!normalized || seen.has(normalized)) {        return false;      }      seen.add(normalized);      return true;    });  }  function getPrimaryLink(item, titleLink, ctaLink, thumbLink) {    var candidate = titleLink || ctaLink || thumbLink;    if (candidate && candidate.getAttribute('href')) {      return candidate.getAttribute('href');    }    return '';  }  function extractImageData(item) {    var img = item.querySelector('.summary-thumbnail-image, img');    var thumb = item.querySelector('.summary-thumbnail, .summary-thumbnail-container, .summary-thumbnail-outer-container, .img-wrapper');    var srcCandidates = [];    if (img) {      srcCandidates.push(img.currentSrc || '');      srcCandidates.push(img.getAttribute('src') || '');      srcCandidates.push(img.getAttribute('data-src') || '');      srcCandidates.push(img.getAttribute('data-image') || '');      srcCandidates.push(img.getAttribute('data-image-url') || '');      srcCandidates.push(pickFromSrcset(img.getAttribute('srcset')));      srcCandidates.push(pickFromSrcset(img.getAttribute('data-srcset')));    }    srcCandidates.push(extractBackgroundImage(thumb));    var src = srcCandidates.find(function (candidate) {      return !!candidate;    }) || '';    return {      src: src,      alt: (img && img.getAttribute('alt')) || ''    };  }  function extractItems(summaryBlock) {    var items = Array.from(summaryBlock.querySelectorAll('.summary-item'));    return items      .map(function (item, index) {        var titleLink = item.querySelector('.summary-title-link, .summary-title a');        var titleNode = item.querySelector('.summary-title');        var excerptNode = item.querySelector('.summary-excerpt');        var ctaLink = item.querySelector(          '.summary-read-more-link, .summary-item-read-more-link, .summary-button-container a, .sqs-block-button-element, a[class*="read-more"]'        );        var thumbLink = item.querySelector('.summary-thumbnail-container[href], .summary-thumbnail-outer-container a[href]');        var image = extractImageData(item);        var metaValues = dedupeText(          Array.from(item.querySelectorAll('.summary-metadata-item')).map(function (node) {            var dateTime = node.getAttribute('datetime');            var text = (node.textContent || '').trim();            return text || dateTime || '';          })        );        var title = (titleLink && titleLink.textContent) || (titleNode && titleNode.textContent) || '';        var excerptHtml = excerptNode ? excerptNode.innerHTML.trim() : '';        var cta = null;        if (ctaLink && ctaLink.textContent.trim()) {          cta = {            text: ctaLink.textContent.trim(),            href: ctaLink.getAttribute('href') || '',            target: ctaLink.getAttribute('target') || '',            rel: ctaLink.getAttribute('rel') || ''          };        }        var href = getPrimaryLink(item, titleLink, ctaLink, thumbLink);        return {          index: index,          title: title.trim(),          titleHtml: titleLink ? titleLink.innerHTML.trim() : title.trim(),          excerptHtml: excerptHtml,          excerptText: excerptNode ? excerptNode.textContent.trim() : '',          href: href,          target: (titleLink && titleLink.getAttribute('target')) || '',          rel: (titleLink && titleLink.getAttribute('rel')) || '',          imageSrc: image.src,          imageAlt: image.alt || title.trim(),          cta: cta,          meta: metaValues        };      })      .filter(function (item) {        return !!(item.title || item.excerptText || item.imageSrc || item.href);      });  }  function createInlineImage(data) {    var wrap = document.createElement('div');    wrap.className = 'irh-inline-image-wrap';    if (!data.imageSrc) {      wrap.classList.add('is-empty');      return wrap;    }    var image = document.createElement('img');    image.className = 'irh-inline-image';    image.loading = 'lazy';    image.decoding = 'async';    image.src = data.imageSrc;    image.alt = data.imageAlt || '';    wrap.appendChild(image);    return wrap;  }  function appendLinkAttributes(link, data) {    if (data.target) {      link.setAttribute('target', data.target);    }    if (data.rel) {      link.setAttribute('rel', data.rel);    }  }  function createTitleElement(data, useRollover) {    var wrap = document.createElement('div');    wrap.className = 'irh-title-wrap';    var primary;    if (data.href) {      primary = document.createElement('a');      primary.href = data.href;      appendLinkAttributes(primary, data);    } else {      primary = document.createElement('span');    }    primary.className = 'irh-title irh-title-primary';    primary.innerHTML = data.titleHtml || data.title;    wrap.appendChild(primary);    if (useRollover) {      var ghost = document.createElement('span');      ghost.className = 'irh-title irh-title-ghost';      ghost.innerHTML = data.titleHtml || data.title;      wrap.appendChild(ghost);      wrap.classList.add('is-rollover');    }    return wrap;  }  function createMetaElement(meta) {    if (!meta || !meta.length) {      return null;    }    var block = document.createElement('div');    block.className = 'irh-meta';    block.textContent = meta.join(' | ');    return block;  }  function createExcerptElement(excerptHtml) {    if (!excerptHtml) {      return null;    }    var excerpt = document.createElement('div');    excerpt.className = 'irh-desc';    excerpt.innerHTML = excerptHtml;    return excerpt;  }  function createCtaElement(cta) {    if (!cta || !cta.text) {      return null;    }    var ctaLink = document.createElement('a');    ctaLink.className = 'irh-cta';    ctaLink.textContent = cta.text;    if (cta.href) {      ctaLink.href = cta.href;    }    if (cta.target) {      ctaLink.target = cta.target;    }    if (cta.rel) {      ctaLink.rel = cta.rel;    }    return ctaLink;  }  function createTextColumn(data, config, forAccordion) {    var text = document.createElement('div');    text.className = 'irh-text';    text.appendChild(createTitleElement(data, config.textEffects.rollover && !forAccordion));    var meta = createMetaElement(data.meta);    if (meta) {      text.appendChild(meta);    }    var excerpt = createExcerptElement(data.excerptHtml);    if (excerpt) {      text.appendChild(excerpt);    }    var cta = createCtaElement(data.cta);    if (cta) {      text.appendChild(cta);    }    return text;  }  function createRow(data, instance, rowIndex) {    var config = instance.config;    var row = document.createElement('article');    var panelId = 'irh-panel-' + instance.id + '-' + rowIndex;    var triggerId = 'irh-trigger-' + instance.id + '-' + rowIndex;    row.className = 'irh-row';    row.setAttribute('role', 'listitem');    row.setAttribute('tabindex', '0');    row.setAttribute('data-row-index', String(rowIndex));    row.setAttribute('data-irh-img', data.imageSrc || '');    row.setAttribute('data-irh-alt', data.imageAlt || '');    var trigger = document.createElement('button');    trigger.type = 'button';    trigger.className = 'irh-accordion-trigger';    trigger.id = triggerId;    trigger.setAttribute('aria-expanded', 'false');    trigger.setAttribute('aria-controls', panelId);    trigger.textContent = data.title || 'Untitled';    row.appendChild(trigger);    var main = document.createElement('div');    main.className = 'irh-row-main';    main.appendChild(createInlineImage(data));    main.appendChild(createTextColumn(data, config, false));    row.appendChild(main);    var panel = document.createElement('div');    panel.className = 'irh-accordion-panel';    panel.id = panelId;    panel.setAttribute('role', 'region');    panel.setAttribute('aria-labelledby', triggerId);    panel.hidden = true;    var panelInner = document.createElement('div');    panelInner.className = 'irh-accordion-inner';    panelInner.appendChild(createInlineImage(data));    panelInner.appendChild(createTextColumn(data, config, true));    panel.appendChild(panelInner);    row.appendChild(panel);    return row;  }  function createPreview(config) {    var preview = document.createElement('div');    preview.className = 'irh-preview';    preview.setAttribute('aria-hidden', 'true');    preview.setAttribute('data-preset', config.animation.preset);    preview.style.setProperty('--irh-duration', config.animation.durationMs + 'ms');    preview.style.setProperty('--irh-easing', config.animation.easing);    var frame = document.createElement('div');    frame.className = 'irh-preview-frame';    var img = document.createElement('img');    img.className = 'irh-preview-img';    img.alt = '';    img.decoding = 'async';    frame.appendChild(img);    preview.appendChild(frame);    return {      el: preview,      img: img    };  }  function isMobileViewport(instance) {    var forceMode = instance.config.mobile && instance.config.mobile.forceMode;    if (forceMode === 'mobile') {      return true;    }    if (forceMode === 'desktop') {      return false;    }    return instance.media.mobile.matches;  }  function shouldUseDesktopHover(instance) {    var isMobile = isMobileViewport(instance);    if (!isMobile) {      return true;    }    return false;  }  function shouldEnableTouchMode(instance) {    return isMobileViewport(instance) && instance.config.mobile.showEffectOnMobile && !instance.config.mobile.accordionMode;  }  function shouldEnableAccordion(instance) {    return isMobileViewport(instance) && instance.config.mobile.accordionMode;  }  function getEffectivePreset(instance) {    if (instance.media.reduced.matches) {      return 'fade';    }    return instance.config.animation.preset;  }  function getEffectiveMode(instance) {    if (instance.media.reduced.matches) {      return 'fixed';    }    return instance.config.reveal.mode;  }  function resolvePreviewSide(instance, rowIndex) {    var layout = instance.config.layout;    var alternate = layout === 'alternating-centered';    if (alternate) {      return rowIndex % 2 === 0 ? 'right' : 'left';    }    if (layout === 'right-content-left-image') {      return 'left';    }    if (layout === 'center-image') {      return 'center';    }    if (layout === 'title-left-image-custom') {      return 'custom-right';    }    return 'right';  }  function getPreviewMetrics(instance) {    var rect = instance.preview.el.getBoundingClientRect();    var width = rect.width || 320;    var height = rect.height || 440;    return { width: width, height: height };  }  function computePreviewPosition(instance, row, event) {    var margin = 16;    var gap = 18;    var viewportWidth = window.innerWidth;    var viewportHeight = window.innerHeight;    var rowRect = row.getBoundingClientRect();    var metrics = getPreviewMetrics(instance);    var rowIndex = Number(row.getAttribute('data-row-index')) || 0;    var side = resolvePreviewSide(instance, rowIndex);    var mode = getEffectiveMode(instance);    var x;    var y = rowRect.top - instance.config.reveal.hoverLiftY + instance.config.reveal.offsetY;    if (mode === 'followCursor' && event) {      x = event.clientX + gap + instance.config.reveal.offsetX;      y = event.clientY - metrics.height * 0.45 + instance.config.reveal.offsetY;    } else {      if (side === 'left') {        x = rowRect.left - metrics.width - gap + instance.config.reveal.offsetX;      } else if (side === 'center') {        x = rowRect.left + (rowRect.width - metrics.width) / 2 + instance.config.reveal.offsetX;      } else if (side === 'custom-right') {        x = viewportWidth - metrics.width - instance.config.reveal.offsetRightPx + instance.config.reveal.offsetX;      } else {        x = rowRect.right + gap + instance.config.reveal.offsetX;      }    }    if (x + metrics.width + margin > viewportWidth && side === 'right') {      x = rowRect.left - metrics.width - gap;    }    if (x < margin && side === 'left') {      x = rowRect.right + gap;    }    x = clamp(x, margin, viewportWidth - metrics.width - margin);    y = clamp(y, margin, viewportHeight - metrics.height - margin);    return { x: x, y: y, side: side };  }  function setPreviewImage(instance, row) {    var src = row.getAttribute('data-irh-img') || '';    var alt = row.getAttribute('data-irh-alt') || '';    if (!src) {      return false;    }    if (instance.preview.img.getAttribute('src') !== src) {      instance.preview.img.setAttribute('src', src);    }    instance.preview.img.setAttribute('alt', alt);    return true;  }  function renderPreviewPosition(instance) {    var state = instance.state;    instance.preview.el.style.transform = 'translate3d(' + state.currentX + 'px, ' + state.currentY + 'px, 0)';  }  function stopAnimationLoop(instance) {    if (instance.state.rafId) {      window.cancelAnimationFrame(instance.state.rafId);      instance.state.rafId = 0;    }  }  function startAnimationLoop(instance) {    if (instance.state.rafId) {      return;    }    var tick = function () {      var state = instance.state;      if (!state.activeRow) {        state.rafId = 0;        return;      }      state.currentX += (state.targetX - state.currentX) * 0.16;      state.currentY += (state.targetY - state.currentY) * 0.16;      renderPreviewPosition(instance);      state.rafId = window.requestAnimationFrame(tick);    };    instance.state.rafId = window.requestAnimationFrame(tick);  }  function unbindMouseTracking(instance) {    if (!instance.state.mouseTrackingBound) {      return;    }    window.removeEventListener('mousemove', instance.handlers.onMouseMove, { passive: true });    instance.state.mouseTrackingBound = false;  }  function bindMouseTracking(instance) {    if (instance.state.mouseTrackingBound) {      return;    }    window.addEventListener('mousemove', instance.handlers.onMouseMove, { passive: true });    instance.state.mouseTrackingBound = true;  }  function clearActiveRows(instance) {    instance.rows.forEach(function (row) {      row.classList.remove('is-active', 'is-mobile-open');    });  }  function hidePreview(instance) {    instance.state.activeRow = null;    instance.preview.el.classList.remove('is-visible');    clearActiveRows(instance);    unbindMouseTracking(instance);    stopAnimationLoop(instance);  }  function showPreview(instance, row, event) {    if (!setPreviewImage(instance, row)) {      hidePreview(instance);      return;    }    clearActiveRows(instance);    row.classList.add('is-active');    instance.preview.el.setAttribute('data-preset', getEffectivePreset(instance));    instance.preview.el.style.setProperty('--irh-duration', instance.config.animation.durationMs + 'ms');    instance.preview.el.style.setProperty('--irh-easing', instance.config.animation.easing);    var rotation = instance.config.reveal.rotationDeg || 0;    instance.preview.el.style.setProperty('--irh-rotation', rotation + 'deg');    var initialPos = computePreviewPosition(instance, row, event);    var state = instance.state;    state.activeRow = row;    state.baseX = initialPos.x;    state.baseY = initialPos.y;    state.targetX = initialPos.x;    state.targetY = initialPos.y;    state.currentX = initialPos.x;    state.currentY = initialPos.y;    renderPreviewPosition(instance);    instance.preview.el.classList.add('is-visible');    var mode = getEffectiveMode(instance);    if (mode === 'drift' || mode === 'followCursor') {      bindMouseTracking(instance);      startAnimationLoop(instance);    }  }  function handleMouseMove(instance, event) {    var state = instance.state;    var row = state.activeRow;    if (!row) {      return;    }    var mode = getEffectiveMode(instance);    var base = computePreviewPosition(instance, row, event);    state.baseX = base.x;    state.baseY = base.y;    if (mode === 'followCursor') {      state.targetX = base.x;      state.targetY = base.y;      return;    }    if (mode === 'drift') {      var rect = row.getBoundingClientRect();      var cx = rect.left + rect.width / 2;      var cy = rect.top + rect.height / 2;      var dx = clamp((event.clientX - cx) / Math.max(rect.width, 1), -1, 1);      var dy = clamp((event.clientY - cy) / Math.max(rect.height, 1), -1, 1);      state.targetX = base.x + dx * 18;      state.targetY = base.y + dy * 14;    }  }  function setTouchRowState(instance, row, open) {    row.classList.toggle('is-mobile-open', open);  }  function closeAllTouchRows(instance) {    instance.rows.forEach(function (row) {      row.classList.remove('is-mobile-open');    });  }  function setAccordionRowState(row, open) {    var trigger = row.querySelector('.irh-accordion-trigger');    var panel = row.querySelector('.irh-accordion-panel');    if (!trigger || !panel) {      return;    }    row.classList.toggle('is-accordion-open', open);    trigger.setAttribute('aria-expanded', open ? 'true' : 'false');    if (open) {      panel.hidden = false;      panel.style.maxHeight = panel.scrollHeight + 'px';    } else {      panel.style.maxHeight = panel.scrollHeight + 'px';      window.requestAnimationFrame(function () {        panel.style.maxHeight = '0px';      });      window.setTimeout(function () {        if (!row.classList.contains('is-accordion-open')) {          panel.hidden = true;        }      }, 220);    }  }  function closeAllAccordionRows(instance, exceptRow) {    instance.rows.forEach(function (row) {      if (row !== exceptRow) {        setAccordionRowState(row, false);      }    });  }  function toggleAccordionRow(instance, row) {    var isOpen = row.classList.contains('is-accordion-open');    if (instance.config.mobile.accordionSingleOpen) {      closeAllAccordionRows(instance, row);    }    setAccordionRowState(row, !isOpen);  }  function applyRootConfig(instance) {    var config = instance.config;    var root = instance.root;    root.setAttribute('data-layout', config.layout);    root.setAttribute('data-mobile-effect', config.mobile.showEffectOnMobile ? 'true' : 'false');    root.setAttribute('data-accordion', config.mobile.accordionMode ? 'true' : 'false');    root.setAttribute('data-row-bg', config.rowHover.bgEnabled ? 'true' : 'false');    var inward = config.textEffects.inwardSlidePx;    var inwardSigned =      config.layout === 'right-content-left-image' ? String(-Math.abs(inward)) : String(Math.abs(inward));    root.style.setProperty('--irh-inward-slide', inwardSigned + 'px');    root.style.setProperty('--irh-row-hover-bg', config.rowHover.bgColor);  }  function attachRowHandlers(instance, row) {    var onPointerEnter = function (event) {      if (!shouldUseDesktopHover(instance)) {        return;      }      showPreview(instance, row, event);    };    var onPointerLeave = function () {      if (!shouldUseDesktopHover(instance)) {        return;      }      hidePreview(instance);    };    var onFocusIn = function () {      if (!shouldUseDesktopHover(instance)) {        return;      }      showPreview(instance, row);    };    var onFocusOut = function (event) {      if (!shouldUseDesktopHover(instance)) {        return;      }      if (row.contains(event.relatedTarget)) {        return;      }      hidePreview(instance);    };    var onKeyDown = function (event) {      var key = event.key;      var isTriggerKey = key === 'Enter' || key === ' ';      if (!isTriggerKey) {        return;      }      if (event.target.closest('a, button, input, textarea, select')) {        return;      }      if (shouldEnableAccordion(instance)) {        event.preventDefault();        toggleAccordionRow(instance, row);        return;      }      if (shouldEnableTouchMode(instance)) {        event.preventDefault();        var open = !row.classList.contains('is-mobile-open');        closeAllTouchRows(instance);        setTouchRowState(instance, row, open);      }    };    var onClick = function (event) {      if (!isMobileViewport(instance)) {        return;      }      if (shouldEnableAccordion(instance)) {        if (event.target.closest('.irh-accordion-trigger')) {          event.preventDefault();          toggleAccordionRow(instance, row);        }        return;      }      if (shouldEnableTouchMode(instance)) {        if (event.target.closest('a, button')) {          return;        }        event.preventDefault();        var open = !row.classList.contains('is-mobile-open');        closeAllTouchRows(instance);        setTouchRowState(instance, row, open);      }    };    row.addEventListener('pointerenter', onPointerEnter);    row.addEventListener('pointerleave', onPointerLeave);    row.addEventListener('focusin', onFocusIn);    row.addEventListener('focusout', onFocusOut);    row.addEventListener('keydown', onKeyDown);    row.addEventListener('click', onClick);    instance.cleanup.push(function () {      row.removeEventListener('pointerenter', onPointerEnter);      row.removeEventListener('pointerleave', onPointerLeave);      row.removeEventListener('focusin', onFocusIn);      row.removeEventListener('focusout', onFocusOut);      row.removeEventListener('keydown', onKeyDown);      row.removeEventListener('click', onClick);    });  }  function createInstance(context) {    var section = context.section;    var summaryBlock = context.summaryBlock;    var id = context.id;    var config = context.config;    var items = context.items;    var root = document.createElement('div');    root.className = 'irh-root';    root.setAttribute('data-irh-instance', id);    var rows = document.createElement('div');    rows.className = 'irh-rows';    rows.setAttribute('role', 'list');    root.appendChild(rows);    var preview = createPreview(config);    document.body.appendChild(preview.el);    var instance = {      id: id,      config: config,      section: section,      source: summaryBlock,      root: root,      rows: [],      cleanup: [],      preview: preview,      handlers: {},      media: {        mobile: window.matchMedia(MOBILE_QUERY),        reduced: window.matchMedia(REDUCED_MOTION_QUERY)      },      state: {        activeRow: null,        mouseTrackingBound: false,        currentX: 0,        currentY: 0,        targetX: 0,        targetY: 0,        baseX: 0,        baseY: 0,        rafId: 0      }    };    applyRootConfig(instance);    items.forEach(function (itemData, rowIndex) {      var row = createRow(itemData, instance, rowIndex);      rows.appendChild(row);      instance.rows.push(row);      attachRowHandlers(instance, row);    });    instance.handlers.onMouseMove = function (event) {      handleMouseMove(instance, event);    };    var onDocumentPointerDown = function (event) {      if (!isMobileViewport(instance)) {        return;      }      if (instance.root.contains(event.target)) {        return;      }      closeAllTouchRows(instance);      if (shouldEnableAccordion(instance)) {        closeAllAccordionRows(instance, null);      }    };    document.addEventListener('pointerdown', onDocumentPointerDown);    instance.cleanup.push(function () {      document.removeEventListener('pointerdown', onDocumentPointerDown);    });    var onResize = function () {      if (!isMobileViewport(instance)) {        closeAllTouchRows(instance);        closeAllAccordionRows(instance, null);      }      if (instance.state.activeRow) {        showPreview(instance, instance.state.activeRow);      }    };    window.addEventListener('resize', onResize, { passive: true });    instance.cleanup.push(function () {      window.removeEventListener('resize', onResize);    });    summaryBlock.classList.add('irh-source-hidden');    summaryBlock.setAttribute('data-irh-source-hidden', 'true');    summaryBlock.insertAdjacentElement('afterend', root);    return instance;  }  function destroyInstance(instance) {    hidePreview(instance);    instance.cleanup.forEach(function (fn) {      fn();    });    instance.cleanup.length = 0;    if (instance.root && instance.root.parentNode) {      instance.root.parentNode.removeChild(instance.root);    }    if (instance.preview && instance.preview.el && instance.preview.el.parentNode) {      instance.preview.el.parentNode.removeChild(instance.preview.el);    }    if (instance.source) {      instance.source.classList.remove('irh-source-hidden');      instance.source.removeAttribute('data-irh-source-hidden');    }    instances.delete(instance.id);  }  function buildContexts(config) {    var containers = resolveContainers(config.target);    var contexts = [];    containers.forEach(function (container, containerIndex) {      var summaryBlocks = findSummaryBlocks(container);      summaryBlocks.forEach(function (summaryBlock, blockIndex) {        var items = extractItems(summaryBlock);        if (!items.length) {          return;        }        var baseId = config.instanceId || container.id || nextInstanceId('irh');        var suffix = containerIndex + '-' + blockIndex;        var resolvedId = summaryBlocks.length > 1 || containers.length > 1 ? baseId + '-' + suffix : baseId;        contexts.push({          id: resolvedId,          section: container,          summaryBlock: summaryBlock,          config: config,          items: items        });      });    });    return contexts;  }  function init(userConfig) {    var config = normalizeConfig(userConfig || {});    var contexts = buildContexts(config);    var created = [];    contexts.forEach(function (context) {      if (instances.has(context.id)) {        destroyInstance(instances.get(context.id));      }      var instance = createInstance(context);      instances.set(instance.id, instance);      created.push(instance.id);    });    return created;  }  function destroy(instanceId) {    if (!instanceId) {      Array.from(instances.values()).forEach(function (instance) {        destroyInstance(instance);      });      return;    }    if (instances.has(instanceId)) {      destroyInstance(instances.get(instanceId));      return;    }    Array.from(instances.values())      .filter(function (instance) {        return instance.id.indexOf(instanceId) === 0;      })      .forEach(function (instance) {        destroyInstance(instance);      });  }  function refresh(userConfig) {    destroy();    return init(userConfig || {});  }  window.ImageRevealHover = {    init: init,    destroy: destroy,    refresh: refresh,    version: '1.0.0'  };})(window, document);
